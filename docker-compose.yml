version: "3.9"

services:
  baese:
    container_name: PostgresDBMeileaf
    image: postgres:latest
    restart: on-failure
    environment:
      POSTGRES_USER: postgres1
      POSTGRES_PASSWORD: passwort1
      POSTGRES_DB: Teeshop
    ports:
      - 5432:5432
