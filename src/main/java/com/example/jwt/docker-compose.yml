version: "3.9"

services:
  baese:
    container_name: PostgresDB
    image: postgres:latest
    restart: on-failure
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: passwort
      POSTGRES_DB: Teeshop
    ports:
      - 5432:5432
